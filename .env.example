# Firebase Configuration
# These environment variables are required for Firebase authentication

# Firebase Project Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyAhTrqodNYzKNK-RmIoiGtiSPE3odobA_w
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=atat-a51a6.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=atat-a51a6
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=atat-a51a6.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=512547275450
NEXT_PUBLIC_FIREBASE_APP_ID=1:512547275450:web:f6dc0a16b8332baedd3883

# Firebase Mock Mode
# Set to "true" to enable mock mode for development or when Firebase is unavailable
# Leave commented out to use real Firebase authentication (requires valid credentials)
# NEXT_PUBLIC_FIREBASE_MOCK_MODE=true

# Admin Configuration
NEXT_PUBLIC_ADMIN_EMAIL=admin@example.com

# Instructions:
# 1. Copy this file to .env.local for development
# 2. Replace the values with your actual Firebase project credentials
# 3. Keep the .env.local file out of version control (it's already in .gitignore)
# 4. For production, set these environment variables in your hosting platform
#    (e.g., Vercel, Netlify)
# 5. In development, you can use mock mode by uncommenting NEXT_PUBLIC_FIREBASE_MOCK_MODE

# Mock Credentials (only usable when mock mode is enabled):
# Regular User: demo@example.com / password
# Admin User: admin@example.com / admin123

# Note: All variables prefixed with NEXT_PUBLIC_ will be exposed to the browser.
# For server-side variables that should not be exposed, remove the NEXT_PUBLIC_ prefix. 